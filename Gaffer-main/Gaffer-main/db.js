// Legacy placeholder. The Live Server version does not use PostgreSQL.
// Club data is loaded by local-db.js from data/seed.json and persisted in
// the browser's localStorage. The old hard-coded remote credentials were
// intentionally removed from this archived file.
